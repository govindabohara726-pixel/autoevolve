@extends('layouts.admin') @section('title','Content') @section('content')<div class="page-head"><div><p class="eyebrow">CONTENT ENGINE</p><h1>Content</h1><p class="muted">Generate drafts, edit structured content, publish and preserve versions.</p></div></div><div class="panel"><h2>Generate AI draft</h2><form class="inline-form" method="post" action="{{ route('admin.content.generate') }}">@csrf<input name="topic" placeholder="e.g. Best AI note-taking app for students" required><select name="type"><option>guide</option><option>comparison</option><option>alternatives</option><option>best-of</option><option>how-to</option><option>decision</option></select><button class="button">Generate</button></form></div><div class="panel"><div class="table-wrap"><table><thead><tr><th>Title</th><th>Status</th><th>Health</th><th>SEO</th><th>Updated</th></tr></thead><tbody>@forelse($items as $item)<tr><td><a href="{{ route('admin.content.edit',$item) }}"><strong>{{ $item->title }}</strong></a><div class="tiny">/{{ $item->slug }}</div></td><td><span class="pill">{{ $item->status }}</span></td><td>{{ $item->health_score }}</td><td>{{ $item->seo_score }}</td><td>{{ $item->updated_at->diffForHumans() }}</td></tr>@empty<tr><td colspan="5" class="muted">No content yet. Generate the first draft above.</td></tr>@endforelse</tbody></table></div>{{ $items->links() }}</div>@endsection

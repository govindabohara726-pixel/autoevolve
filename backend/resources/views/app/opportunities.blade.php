@extends('layouts.app')
@section('title','Opportunities')
@section('content')
<div class="page-head"><div><span class="eyebrow">TOPIC DISCOVERY</span><h1>Opportunities</h1><p>{{ $site->name }} · AI-generated adjacent content ideas ranked by internal relevance—not fabricated search-volume claims.</p></div><form method="post" action="{{ route('app.opportunities.discover') }}">@csrf<button class="btn">Discover opportunities</button></form></div>
<div class="grid-3">@forelse($items as $item)<article class="data-card" style="padding:20px"><div class="panel-head"><span class="pill">{{ $item->type }}</span><span class="pill {{ $item->status==='new'?'green':'' }}">{{ $item->status }}</span></div><h3 style="font-size:19px;margin:16px 0 7px">{{ $item->topic }}</h3><div class="muted" style="font-size:12px;margin-bottom:12px">{{ $item->keyword ?: 'No target keyword yet' }}</div><p class="muted">{{ $item->reason }}</p><div class="kpi-row"><span>Priority</span><strong>{{ $item->priority }}/100</strong></div>@if($item->status==='new')<form method="post" action="{{ route('app.opportunities.dismiss',$item) }}" style="margin-top:14px">@csrf<button class="btn ghost small">Dismiss</button></form>@endif</article>@empty<div class="panel" style="grid-column:1/-1"><div class="empty">No opportunities yet. Run discovery to build a content gap queue.</div></div>@endforelse</div><div class="pagination">{{ $items->links() }}</div>
@endsection
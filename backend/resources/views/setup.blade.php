<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Backend setup · AutoEvolve</title><link rel="stylesheet" href="{{ asset('admin.css') }}"></head><body class="setup-page"><main class="setup-wrap"><p class="eyebrow">LARAVEL BACKEND HEALTH</p><h1>Finish connecting AutoEvolve</h1><p class="muted">This page shows configuration state only. Secret values are never displayed.</p><div class="panel"><h2>Backend checks</h2>@foreach($checks as $name=>$ok)<div class="check-row"><code>{{ strtoupper($name) }}</code><strong class="{{ $ok?'ok':'bad' }}">{{ $ok?'✓ Ready':'✕ Missing' }}</strong></div>@endforeach</div><div class="panel"><h2>First install</h2><ol><li>Copy <code>.env.example</code> to <code>.env</code> and run <code>php artisan key:generate</code>.</li><li>Run <code>php artisan autoevolve:install --email=you@example.com --password='strong-password'</code>.</li><li>Add <code>AI_API_KEY</code> when you want generation/evolution enabled.</li><li>Point Vercel's <code>BACKEND_URL</code> and <code>NEXT_PUBLIC_BACKEND_URL</code> to this Laravel URL.</li><li>For automation, run <code>* * * * * php artisan schedule:run</code> from server cron.</li></ol></div><p><a class="button" href="{{ route('admin.login') }}">Admin sign in</a></p></main></body></html>

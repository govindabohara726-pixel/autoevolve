import { redirect } from "next/navigation";import { backendUrl } from "@/lib/backend";export const dynamic="force-dynamic";export default function Login(){const b=backendUrl();if(!b)redirect('/setup');redirect(`${b}/admin/login`)}

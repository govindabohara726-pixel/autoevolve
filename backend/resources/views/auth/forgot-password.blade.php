<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Reset password · AutoEvolve</title><link rel="stylesheet" href="{{ asset('saas.css') }}"></head><body><div class="auth-panel" style="min-height:100vh"><div class="auth-card"><a class="brand" href="{{ url('/') }}"><span class="brandmark">↗</span>AutoEvolve</a><div style="height:30px"></div><span class="eyebrow">ACCOUNT RECOVERY</span><h2>Forgot your password?</h2><p>Enter your account email and we’ll send a reset link using the configured mail provider.</p>@if(session('success'))<div class="flash success">{{ session('success') }}</div>@endif @if($errors->any())<div class="flash error">{{ $errors->first() }}</div>@endif<form class="form" method="post" action="{{ route('password.email') }}">@csrf<div class="field"><label>Email</label><input type="email" name="email" value="{{ old('email') }}" required autofocus></div><button class="btn">Send reset link</button></form><p style="margin-top:20px"><a href="{{ route('login') }}" style="color:#a99cff">← Back to sign in</a></p></div></div></body></html>
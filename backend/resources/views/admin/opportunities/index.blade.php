@extends('layouts.admin') @section('title','Opportunities') @section('content')<div class="page-head"><div><p class="eyebrow">TOPIC GRAPH</p><h1>Opportunities</h1><p class="muted">AI-discovered adjacent content gaps. Priority is internal relevance, not claimed search volume.</p></div><form method="post" action="{{ route('admin.opportunities.discover') }}">@csrf<button class="button">Discover now</button></form></div><div class="cards">@forelse($items as $o)<article class="card"><div class="card-top"><span class="pill">{{ $o->type }}</span><strong>{{ $o->priority }}/100</strong></div><h3>{{ $o->topic }}</h3><p>{{ $o->reason }}</p><div class="tiny">Keyword: {{ $o->keyword ?: '—' }} · {{ $o->status }}</div></article>@empty<div class="panel muted">No opportunities yet. Run discovery when the AI key is connected.</div>@endforelse</div>{{ $items->links() }}@endsection

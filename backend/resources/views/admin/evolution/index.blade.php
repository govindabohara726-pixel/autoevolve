@extends('layouts.admin') @section('title','AI activity') @section('content')<div class="page-head"><div><p class="eyebrow">EVOLUTION ENGINE</p><h1>AI activity</h1><p class="muted">Every autonomous action is logged with risk and status.</p></div><form method="post" action="{{ route('admin.evolution.run') }}">@csrf<button class="button">Run cycle</button></form></div><div class="panel"><div class="table-wrap"><table><thead><tr><th>Time</th><th>Action</th><th>Content</th><th>Risk</th><th>Status</th><th>Summary</th></tr></thead><tbody>@forelse($actions as $a)<tr><td>{{ $a->created_at->format('M j H:i') }}</td><td>{{ $a->action_type }}</td><td>{{ optional($a->content)->title ?? 'Site-wide' }}</td><td><span class="pill">{{ $a->risk_level }}</span></td><td>{{ $a->status }}</td><td>{{ $a->summary }}</td></tr>@empty<tr><td colspan="6" class="muted">No actions yet.</td></tr>@endforelse</tbody></table></div>{{ $actions->links() }}</div>@endsection

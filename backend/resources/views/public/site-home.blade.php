<!doctype html><html lang="{{ $site->language ?: 'en' }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $site->name }}</title><meta name="description" content="Latest guides and decision content from {{ $site->name }}."><link rel="stylesheet" href="{{ asset('saas.css') }}"></head><body><header class="marketing-nav"><div class="container"><a class="brand" href="{{ route('public.site',$site) }}"><span class="brandmark">{{ mb_substr($site->name,0,1) }}</span>{{ $site->name }}</a><nav class="marketing-links"><a href="{{ route('public.sitemap',$site) }}">Sitemap</a></nav></div></header><main><section class="hero" style="padding-bottom:45px"><div class="container"><span class="eyebrow">PUBLISHED KNOWLEDGE</span><h1 style="font-size:clamp(42px,7vw,70px)">{{ $site->name }}</h1><p>Guides and decision pages maintained through AutoEvolve's versioned publishing workflow.</p></div></section><section class="section" style="padding-top:20px"><div class="container"><div class="feature-grid">@forelse($items as $item)<article class="feature"><span class="pill">{{ $item->primary_keyword ?: 'Guide' }}</span><h3><a href="{{ route('public.article',[$site,$item->slug]) }}">{{ $item->title }}</a></h3><p>{{ $item->excerpt }}</p><p style="margin-top:18px"><a href="{{ route('public.article',[$site,$item->slug]) }}" style="color:#b8adff">Read page →</a></p></article>@empty<div class="panel" style="grid-column:1/-1"><div class="empty">No published content yet.</div></div>@endforelse</div>@if(method_exists($items,'links'))<div class="pagination">{{ $items->links() }}</div>@endif</div></section></main><footer class="footer"><div class="container"><div>{{ $site->name }}</div><div>Powered by AutoEvolve</div></div></footer></body></html>
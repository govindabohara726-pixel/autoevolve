<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Start free · AutoEvolve</title><link rel="stylesheet" href="{{ asset('saas.css') }}"></head><body><div class="auth-page"><section class="auth-art"><a class="brand" href="{{ url('/') }}"><span class="brandmark">↗</span>AutoEvolve</a><div><span class="eyebrow">14-DAY FREE TRIAL</span><h1>Your first autonomous website workspace starts here.</h1><p class="muted">No credit card required for the trial. Create a workspace, add your site and let AutoEvolve start building an improvement queue.</p></div><p class="muted">Multi-site ready · Team roles · Stripe billing · MySQL storage</p></section><section class="auth-panel"><div class="auth-card"><span class="eyebrow">CREATE ACCOUNT</span><h2>Start building</h2><p>Your workspace and first site are created automatically.</p>@if($errors->any())<div class="flash error">{{ $errors->first() }}</div>@endif<form class="form" method="post" action="{{ route('register.submit') }}">@csrf<div class="field"><label>Your name</label><input name="name" value="{{ old('name') }}" required></div><div class="field"><label>Work email</label><input type="email" name="email" value="{{ old('email') }}" required></div><div class="field"><label>Workspace name</label><input name="workspace_name" value="{{ old('workspace_name') }}" placeholder="My company" required></div><div class="form-row"><div class="field"><label>Password</label><input type="password" name="password" minlength="8" required></div><div class="field"><label>Confirm password</label><input type="password" name="password_confirmation" minlength="8" required></div></div><button class="btn" type="submit">Create workspace</button></form><p style="margin-top:20px">Already have an account? <a href="{{ route('login') }}" style="color:#a99cff">Sign in →</a></p></div></section></div></body></html>
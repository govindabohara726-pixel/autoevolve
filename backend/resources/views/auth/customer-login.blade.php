<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Sign in · AutoEvolve</title><link rel="stylesheet" href="{{ asset('saas.css') }}"></head><body><div class="auth-page"><section class="auth-art"><a class="brand" href="{{ url('/') }}"><span class="brandmark">↗</span>AutoEvolve</a><div><span class="eyebrow">AUTONOMOUS WEBSITE OS</span><h1>Run your content operation without running after it.</h1><p class="muted">Generate, improve, publish and monitor multiple websites from one workspace—with every AI action metered, logged and reversible.</p></div><p class="muted">Secure workspace isolation · Version history · Usage controls</p></section><section class="auth-panel"><div class="auth-card"><span class="eyebrow">WELCOME BACK</span><h2>Sign in to AutoEvolve</h2><p>Continue to your websites, content and automation.</p>@if(session('success'))<div class="flash success">{{ session('success') }}</div>@endif @if($errors->any())<div class="flash error">{{ $errors->first() }}</div>@endif<form class="form" method="post" action="{{ route('login.submit') }}">@csrf<div class="field"><label>Email</label><input type="email" name="email" value="{{ old('email') }}" autocomplete="email" required></div><div class="field"><div class="inline" style="justify-content:space-between"><label>Password</label><a href="{{ route('password.request') }}" style="color:#a99cff;font-size:12px">Forgot password?</a></div><input type="password" name="password" autocomplete="current-password" required></div><label class="inline muted"><input type="checkbox" name="remember" value="1"> Keep me signed in</label><button class="btn" type="submit">Sign in</button></form><p style="margin-top:20px">New here? <a href="{{ route('register') }}" style="color:#a99cff">Start a free trial →</a></p></div></section></div></body></html>
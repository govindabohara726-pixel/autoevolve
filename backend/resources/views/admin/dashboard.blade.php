@extends('layouts.admin') @section('title','Overview') @section('content')<div class="page-head"><div><p class="eyebrow">SYSTEM OVERVIEW</p><h1>Evolution control center</h1><p class="muted">Monitor what the site is publishing, improving and discovering.</p></div><form method="post" action="{{ route('admin.evolution.run') }}">@csrf<button class="button">Run evolution now</button></form></div><div class="stats"><div class="stat"><span>All pages</span><strong>{{ $stats['pages'] }}</strong></div><div class="stat"><span>Published</span><strong>{{ $stats['published'] }}</strong></div><div class="stat"><span>Awaiting approval</span><strong>{{ $stats['pending'] }}</strong></div><div class="stat"><span>New opportunities</span><strong>{{ $stats['opportunities'] }}</strong></div></div><div class="panel"><div class="panel-head"><h2>Recent AI activity</h2><span class="pill">Autonomy {{ $settings->autonomy_level }}/4</span></div><div class="table-wrap"><table><thead><tr><th>Action</th><th>Content</th><th>Risk</th><th>Status</th><th>Summary</th></tr></thead><tbody>@forelse($actions as $a)<tr><td>{{ $a->action_type }}</td><td>{{ optional($a->content)->title ?? 'Site-wide' }}</td><td><span class="pill">{{ $a->risk_level }}</span></td><td>{{ $a->status }}</td><td>{{ $a->summary }}</td></tr>@empty<tr><td colspan="5" class="muted">No AI actions yet.</td></tr>@endforelse</tbody></table></div></div>@endsection

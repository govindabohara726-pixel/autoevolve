@extends('layouts.admin') @section('title','Settings') @section('content')<div class="page-head"><div><p class="eyebrow">AUTONOMY POLICY</p><h1>Settings</h1><p class="muted">Control how aggressively AutoEvolve acts without you.</p></div></div><form class="panel stack settings-form" method="post" action="{{ route('admin.settings.update') }}">@csrf @method('PUT')<label>Site name<input name="site_name" value="{{ $settings->site_name }}" required></label><label>Public site URL<input name="site_url" type="url" value="{{ $settings->site_url }}" placeholder="https://example.com"></label><label>Autonomy level<select name="autonomy_level">@foreach([1=>'Recommend only',2=>'Draft + approval',3=>'Safe auto-publish',4=>'High autonomy'] as $n=>$label)<option value="{{ $n }}" @selected($settings->autonomy_level===$n)>Level {{ $n }} — {{ $label }}</option>@endforeach</select></label><label>Content becomes stale after <input name="stale_after_days" type="number" min="1" max="365" value="{{ $settings->stale_after_days }}"> days</label><label>Maximum actions per cycle<input name="max_actions_per_run" type="number" min="1" max="50" value="{{ $settings->max_actions_per_run }}"></label><label class="check"><input type="checkbox" name="auto_publish_low_risk" value="1" @checked($settings->auto_publish_low_risk)> Allow low-risk automatic publishing</label><p class="tiny">URL deletion, pricing changes and production code changes are intentionally outside the autonomous executor.</p><button class="button">Save settings</button></form>@endsection
